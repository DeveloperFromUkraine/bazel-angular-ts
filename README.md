# NgxBootstrapNewBazel

For `start` use `npm i`

## Start Bazel build

For start `build` use `bazel build //...` or `bazel build //src:ngx-bootstrap`


### Error on build process 

`Error: The angular repository is not installed in your Bazel WORKSPACE file`
